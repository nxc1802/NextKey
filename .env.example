# Copy to .env for local-only values.
# Do not commit secrets, dataset paths containing private data, or model tokens.

NEXTKEY_ENV=local
NEXTKEY_DATA_DIR=data
NEXTKEY_EXPERIMENTS_DIR=experiments
NEXTKEY_MODELS_DIR=models
