# Phase 2 — Topology Sweep: Config C (Narrow/Deep: 90x3) ~300K params
model:
  name: "bigru"
  embed_dim: 48
  hidden_dim: 90
  num_layers: 3
  dropout: 0.1

optimizer:
  type: "AdamW"
  lr: 0.001
  weight_decay: 0.01

scheduler:
  type: "CosineAnnealingLR"
  T_max: 40
  eta_min: 0.00001
