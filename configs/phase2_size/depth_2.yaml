# Phase 2 — Depth Sweep: D2 (2 Layers) ~420K params
model:
  name: "bigru"
  embed_dim: 64
  hidden_dim: 128
  num_layers: 2
  dropout: 0.1

optimizer:
  type: "AdamW"
  lr: 0.001
  weight_decay: 0.01

scheduler:
  type: "CosineAnnealingLR"
  T_max: 40
  eta_min: 0.00001
