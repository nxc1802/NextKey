# Phase 2 — Topology Sweep: Config B (Mid/Mid: 110x2) ~300K params
model:
  name: "bigru"
  embed_dim: 64
  hidden_dim: 110
  num_layers: 2
  dropout: 0.1

optimizer:
  type: "AdamW"
  lr: 0.001
  weight_decay: 0.01

scheduler:
  type: "CosineAnnealingLR"
  T_max: 40
  eta_min: 0.00001
